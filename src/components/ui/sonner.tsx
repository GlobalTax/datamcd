
import React from "react"

type ToasterProps = {
  className?: string;
}

const Toaster = ({ className, ...props }: ToasterProps) => {
  // Componente simplificado sin dependencia de sonner
  // Las notificaciones se manejan a través del sistema centralizado
  return null;
}

export { Toaster }
